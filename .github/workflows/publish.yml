# cipheron — ARM-CE / AES-NI accelerated Telegram crypto via OpenSSL EVP
# Copyright (c) 2024-Present Ankit Chaubey <ankitchaubey.dev@gmail.com>
# GitHub: https://github.com/ankit-chaubey/cipheron
# License: MIT — see LICENSE file for details

name: Publish to PyPI

on:
  release:
    types: [published]          # triggers on GitHub Release publish
  push:
    tags:
      - "v*"                    # triggers on any tag like v0.1.0, v1.0.0
  workflow_dispatch:            # allows manual trigger from GitHub Actions UI
    inputs:
      version:
        description: "Version to publish (e.g. 0.1.0)"
        required: false

permissions:
  id-token: write               # Required for OIDC trusted publisher

jobs:
  build_sdist:
    name: Build source distribution
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install build tools
        run: pip install build

      - name: Build sdist
        run: python -m build --sdist

      - name: Upload sdist artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/*.tar.gz

  publish:
    name: Publish to PyPI
    needs: build_sdist
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/cipheron/

    steps:
      - name: Download sdist artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        # No API token needed — uses OIDC trusted publisher
